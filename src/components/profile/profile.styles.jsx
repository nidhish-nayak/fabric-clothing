import styled from "styled-components";

export const ProfileContainer = styled.div`

    position: relative;
    margin-top: 0.4em;

    img {
        margin-left: 0.5em;
        margin-right: 0.5em;
        padding: 0.25em;
        border-radius: 50%;
        width: 3em;
        cursor: pointer;
    }
`
